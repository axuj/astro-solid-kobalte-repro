{
  "name": "astro-solid-kobalte-repro",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "postinstall": "patch-package",
    "repro:broken": "node scripts/toggle-patch.mjs off && npm install && npm run build",
    "repro:fixed": "node scripts/toggle-patch.mjs on && npm install && npm run build"
  },
  "dependencies": {
    "@astrojs/solid-js": "^7.0.1",
    "@kobalte/core": "^0.13.12",
    "astro": "^7.1.5",
    "solid-js": "^1.9.5",
    "vitefu": "^1.1.3"
  },
  "devDependencies": {
    "patch-package": "^8.0.0"
  }
}
